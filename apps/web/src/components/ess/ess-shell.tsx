import { EssMobileShell } from "@/components/ess/mobile/ess-mobile-shell";

export function EssShell({ children }: { children: React.ReactNode }) {
  return <EssMobileShell>{children}</EssMobileShell>;
}
