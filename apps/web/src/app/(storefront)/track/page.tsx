import { TrackOrderView } from "@/components/storefront/track/track-order-view";

export const metadata = {
  title: "Track Order — AgainShop",
  description: "Track your AgainShop order status and delivery progress.",
};

export default function TrackOrderPage() {
  return <TrackOrderView />;
}
