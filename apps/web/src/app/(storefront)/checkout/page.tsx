import { CheckoutView } from "@/components/storefront/checkout/checkout-view";

export const metadata = {
  title: "Checkout — AgainShop",
  description: "Secure guest checkout",
};

export default function CheckoutPage() {
  return <CheckoutView />;
}
