import { WishlistView } from "@/components/storefront/wishlist/wishlist-view";

export const metadata = {
  title: "Wishlist — AgainShop",
};

export default function WishlistPage() {
  return <WishlistView />;
}
