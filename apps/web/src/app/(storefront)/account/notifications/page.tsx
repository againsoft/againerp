import { AccountNotificationsView } from "@/components/storefront/account/account-notifications-view";

export const metadata = {
  title: "Notifications — My account — AgainShop",
  robots: { index: false, follow: false },
};

export default function AccountNotificationsPage() {
  return <AccountNotificationsView />;
}
