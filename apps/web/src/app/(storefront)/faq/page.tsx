import { FaqView } from "@/components/storefront/faq/faq-view";

export const metadata = {
  title: "FAQ — AgainShop",
  description: "Answers to common questions about orders, shipping, returns, and payments.",
};

export default function FaqPage() {
  return <FaqView />;
}
