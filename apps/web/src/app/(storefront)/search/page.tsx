import { SearchView } from "@/components/storefront/search/search-view";

export const metadata = {
  title: "Search — AgainShop",
};

export default function SearchPage() {
  return <SearchView />;
}
