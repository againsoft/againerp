import { ContactView } from "@/components/storefront/contact/contact-view";

export const metadata = {
  title: "Contact Us — AgainShop",
  description: "Get in touch with AgainShop support for orders, returns, and product questions.",
};

export default function ContactPage() {
  return <ContactView />;
}
