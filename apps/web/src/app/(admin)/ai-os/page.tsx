import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="AI OS"
      description="Chief Agent, workflows, knowledge, monitoring — use header AI drawer for mock assistant."
    />
  );
}
