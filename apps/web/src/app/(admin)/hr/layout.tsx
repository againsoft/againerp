import { HrModuleLayout } from "@/components/hr/hr-module-layout";

export default function HrLayout({ children }: { children: React.ReactNode }) {
  return <HrModuleLayout>{children}</HrModuleLayout>;
}
